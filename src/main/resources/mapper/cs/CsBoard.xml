<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.kr.gaepan.mapper.cs.CsBoardMapper">

    <select id="selectArticles">
        SELECT a.*, b.cateName, c.typeName FROM gp_board a
        JOIN gp_board_cate b ON a.cate = b.cate
        JOIN gp_board_type c ON a.type = c.type
        WHERE a.group = #{group} AND a.cate = #{cate} AND a.type= #{type}
        ORDER BY a.no DESC
    </select>


</mapper>