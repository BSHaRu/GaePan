<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.kr.gaepan.mapper.pet.PetListMapper">

    <select id="petlist" resultType="co.kr.gaepan.dto.pet.PetRegisterDTO">

        SELECT * FROM gp_pet ORDER BY `no` DESC;

    </select>

    <select id="petdoglist" resultType="co.kr.gaepan.dto.pet.PetRegisterDTO">

        SELECT * FROM gp_pet WHERE cate= #{cate} ORDER BY `no` DESC;

    </select>

    <select id="petcatlist" resultType="co.kr.gaepan.dto.pet.PetRegisterDTO">

        SELECT * FROM gp_pet WHERE cate= #{cate} ORDER BY `no` DESC;

    </select>

    <select id="petetclist" resultType="co.kr.gaepan.dto.pet.PetRegisterDTO">

        SELECT * FROM gp_pet WHERE cate= #{cate} ORDER BY `no` DESC;

    </select>

    <select id="petcategory" resultType="co.kr.gaepan.dto.pet.PetCateDTO">

        SELECT * FROM gp_pet_cate

    </select>
    <select id="petType" resultType="co.kr.gaepan.dto.pet.PetTypeDTO">

        SELECT * FROM gp_pet_type WHERE cate= #{cate};

    </select>

    <select id="pet" resultType="co.kr.gaepan.dto.pet.PetRegisterDTO">

        SELECT * FROM gp_pet WHERE `no`= #{no};

    </select>

</mapper>